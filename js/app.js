    $(document).ready(function(){
      $('.sidenav').sidenav();
      // $('.materialboxed').materialbox();
      $('.parallax').parallax();
      $('.tabs').tabs();
      $('.materialboxed').materialbox();
      // $('.datepicker').datepicker({
      //   disableWeekends: true,
      //   yearRange: 1
      // });
      $('.tooltipped').tooltip();
       $('.scrollspy').scrollSpy();

    });